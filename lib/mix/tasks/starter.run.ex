defmodule Mix.Tasks.Starter.Run do
  @shortdoc "Runs your project's starter"

  @moduledoc """
  Finds the starter generated by `mix starter.new` and runs it, passing all
  flags through.

      mix starter.run
      mix starter.run --oban-pro --gigalixir

  Equivalent to invoking the starter task directly (e.g. `mix my_app.starter`).
  """

  use Mix.Task

  @impl Mix.Task
  def run(argv) do
    Mix.Task.run("compile", [])

    case starter_modules() do
      [module] ->
        Mix.Task.run(Mix.Task.task_name(module), argv)

      [] ->
        Mix.raise("""
        No starter found in this project.

        Generate one with:

            mix starter.new
        """)

      modules ->
        tasks = Enum.map_join(modules, "\n", &"    mix #{Mix.Task.task_name(&1)}")

        Mix.raise("""
        Multiple starters found. Run one directly:

        #{tasks}
        """)
    end
  end

  defp starter_modules do
    app = Mix.Project.config()[:app]

    # get_key only works on loaded applications, and `mix compile` does not
    # reliably load the app (Mix version dependent) — load it explicitly.
    Application.load(app)

    case :application.get_key(app, :modules) do
      {:ok, modules} -> find_starter_modules(modules)
      _ -> []
    end
  end

  @doc false
  # Public for testing: filters a module list down to starter task modules.
  def find_starter_modules(modules) do
    Enum.filter(modules, fn module ->
      String.starts_with?(Atom.to_string(module), "Elixir.Mix.Tasks.") and
        Code.ensure_loaded?(module) and
        function_exported?(module, :__starter__?, 0)
    end)
  end
end
